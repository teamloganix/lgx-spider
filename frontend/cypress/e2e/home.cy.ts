describe("Home page", () => {
	// it("loads and shows hero content", () => {
	// 	cy.visit("/");
	// 	cy.contains("Hello").should("exist");
	// });

	// it("version check", () => {
	// 	cy.visit("/");
	// 	cy.get('span').should($id => {
	// 		expect($id.text()).to.equal('0.0.0')
	// 	})
	// });

	// it("backend version check", () => {
	// 	cy.visit("/");
	// 	cy.get('#test').should($id => {
	// 		expect($id.text()).to.equal('0.0.0')
	// 	})
	// });


});
